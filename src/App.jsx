import gsap from 'gsap';
import { ScrollTrigger } from 'gsap/all';
gsap.registerPlugin(ScrollTrigger);

const App = () => {
  return (
    <main className='text-pink'>
      <div>App</div>
    </main>
  );
};
export default App;
